{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plugins-minimal",
  "description": "Minimal opener/process permissions for the main window",
  "windows": ["main"],
  "permissions": [
    "opener:allow-open-url",
    "process:allow-exit",
    "process:allow-restart"
  ]
}
